---
paths:
    - "app/Settings/**"
---

# Settings

## Inject Spatie settings

Type-hint Spatie settings classes (e.g. GeneralSettings) via constructor or method injection instead of resolving them with app().
