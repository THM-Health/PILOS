<VirtualHost *:443>
    <IfFile "/local/certs/fullchain.pem">
        SSLEngine on
        SSLCertificateKeyFile /local/certs/privkey.pem
        SSLCertificateFile /local/certs/fullchain.pem
    </IfFile>

    Include /etc/apache2/sites-common/app.conf
</VirtualHost>
